{
  "id": "if-player",
  "name": "Interactive Fiction",
  "version": "1.0.0",
  "description": "Play Inform / Z-machine & Glulx text adventures with TTS narration and voice commands.",
  "entry": "index.html",
  "served": true,
  "server": "server.js",
  "options": [
    {
      "key": "storiesDir",
      "label": "Stories folder",
      "type": "folder",
      "default": "",
      "help": "Folder anywhere on this PC that holds your story files (.z5 / .z8 / .zblorb / .ulx / .gblorb). Browse to it; every story inside shows up in the on-screen list. Leave blank to use the app's own bundled stories folder."
    },
    {
      "key": "speak",
      "label": "Read the story aloud",
      "type": "bool",
      "default": true,
      "help": "Speak each new passage through the system TTS voice (Settings → TTS/STT). Can be toggled on screen at any time."
    },
    {
      "key": "voiceInput",
      "label": "Voice commands",
      "type": "bool",
      "default": true,
      "help": "Show the Listen control so you can say commands instead of typing them. Uses the system STT (Settings → TTS/STT)."
    },
    {
      "key": "story",
      "label": "Auto-start story (optional)",
      "type": "text",
      "default": "",
      "help": "Optional: a filename from the stories folder to open straight into. Leave blank to browse the on-screen list (the normal way)."
    }
  ],
  "settings": {
    "key": "ifPlayer",
    "title": "Interactive Fiction",
    "options": [
      {
        "key": "ttsHost",
        "label": "TTS host override",
        "type": "text",
        "default": "",
        "advanced": true,
        "help": "Developer override: a Wyoming TTS host just for this app. Blank uses the system TTS server (Settings → TTS/STT)."
      },
      {
        "key": "ttsPort",
        "label": "TTS port override",
        "type": "text",
        "default": "",
        "advanced": true,
        "help": "Blank uses the system setting (default 10200)."
      },
      {
        "key": "sttHost",
        "label": "STT host override",
        "type": "text",
        "default": "",
        "advanced": true,
        "help": "Developer override: a Wyoming STT host just for this app. Blank uses the system STT server (Settings → TTS/STT)."
      },
      {
        "key": "sttPort",
        "label": "STT port override",
        "type": "text",
        "default": "",
        "advanced": true,
        "help": "Blank uses the system setting (default 10300)."
      }
    ]
  }
}
