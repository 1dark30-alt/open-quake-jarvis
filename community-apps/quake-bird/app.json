{
  "id": "quake-bird",
  "name": "Quake Bird",
  "entry": "bird.html",
  "options": [
    {
      "key": "playerName",
      "label": "Player tag (optional)",
      "type": "text",
      "default": "",
      "maxLength": 16,
      "help": "Overrides the initials entered in the game. Scores are recorded under this tag on the shared high-score server (arcade style: same tag = same player)."
    },
    {
      "key": "serverUrl",
      "label": "Server URL (advanced)",
      "type": "text",
      "default": "https://scores.doofenshmirtzevil.com",
      "help": "Advanced: base URL of the shared score server. Clear it to play offline — your best then stays on this PC."
    }
  ]
}
